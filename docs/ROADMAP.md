# creator-outreach — 开发计划

> 已完成项按交付主题汇总；详细变更可通过 Git 历史追溯。

## 已完成

- [x] **Agent、插件、Skills 与话术基础**: 完成 TikTok Creator Outreach Agent、仅通过 ScoreHub MCP 调用的约束、插件清单、搜索/分析/批量建联 Skills，以及 A/B/C 建联话术模板（2026-07-09 ~ 2026-07-15）。
- [x] **达人搜索与分析质量**: 完成评分模型及 MCP 数据对接、类目两步解析与层级约束修正，并将达人列表固化为表格化输出（2026-07-09 ~ 2026-07-15）。
- [x] **生产入口与商家错误引导**: 切换生产 MCP 域名，并按结构化错误类型分别引导店铺重新绑定、限流等待或修正输入，不暴露错误码（2026-07-15）。
- [x] **安装、MCP 可用性与凭证托管**: 明确支持的客户端和安装前校验，保障 MCP 拉取与不可用恢复提示，并移除面向终端用户的 TikTok 凭证直连降级（2026-07-16）。
- [x] **首次使用与 WorkBuddy 品牌体验**: 补齐首轮能力引导、后续操作建议和 ScoreHub AI 插件品牌/唯一入口，并将头像改为不透明纯白背景，兼容 WorkBuddy 对透明像素的深色渲染（2026-07-16）。
- [x] **发布元数据同步**: 为用户错误引导与 WorkBuddy 白底头像兼容修复升级对外包版本并同步发布元数据（2026-07-15 ~ 2026-07-16）。
- [x] **账号与店铺切换自然语言支持**: 识别切换店铺 / 账号意图，店铺切换直接调用免登录确认页，账号切换才重新登录；新会话首轮对此类会话准备操作豁免欢迎语（2026-07-17）。
- [x] **授权完成店铺信息告知**: 在授权、重新授权或店铺切换成功后，向用户明确告知当前店铺的名称、国家和绑定品牌（2026-07-17）。
- [x] **授权完成账号与店铺信息告知**: 在固定授权确认话术中优先告知当前登录手机号，再告知店铺、国家和品牌（2026-07-17）。
- [x] **搜索达人结果格式约束**: 固定搜索摘要、展示字段、默认顺序、分页状态、双 ID 透传与缺失值处理；1–5 条使用 Markdown，6 条起在 WorkBuddy 可视化可用时生成安全的自包含 HTML（2026-07-17）。
- [x] **达人评分结果格式约束**: 固定多人排行榜、维度分、详情、失败名单和并列排序，单达人不输出误导性的相对综合分；支持 6 人起的 WorkBuddy HTML 报告与完整 Markdown 回退（2026-07-17）。
- [x] **Tiky 品牌定位与智能体介绍统一**: 以 ScoreHub AI 为主品牌、Tiky 为智能体名称，对外统一使用“TikTok达人营销专家”身份，同步 Agent、插件市场、npm 元数据与中英文文档介绍（2026-07-20）。

## 待开发

### 中优先级

- [ ] **配额感知**: 基于已知日配额上限（10,000/天）做预算提示；当远程工具返回 `quota_exhausted` 时停止并提示等待，不再承诺“每次操作前实时检查剩余额度”
- [ ] **竞品达人搜索**: 输入竞品品牌名，搜索有该品牌合作记录的达人，结合评分模型筛选出高潜力名单
  - 复用现有 `search_creators`（keyword 传入品牌名）
  - 复用 `tiktok-creator-analysis` Skill 进行评分
  - hashtag 搜索可作为实验性补充，不作为默认能力承诺

### 低优先级

- [ ] **相似达人推荐**: 基于 `search_creators` + `creator_performance` 实现“以某达人为模板找相似达人”的 MVP 能力，采用多路召回 + 加权评分排序；方案细节见 `docs/tiktok-creator-similar-finder-research.md`

## 暂不做

- **消息监听（达人回复接收/分类/状态机）**: TikTok 无 GET 轮询，接收依赖 Webhook (Type 33)，且需消息落库；本包无数据库、禁止数据修改，暂不纳入本包范围。
- **A/B 测试建联话术**: 追踪回复率需要持久化发送记录和回复结果，超出当前包“无自有数据库、禁止数据修改”的约束。
- **导出功能**: 当前包聚焦对话式筛选、分析和建联流程，不再单独承诺 CSV/Excel 导出能力。

## 跨包依赖

- **`create_conversation` MCP 工具**（批量建联前置）已沿依赖链补齐：sdk (`createConversation`) → mcp-remote (`/mcp/tools/create-conversation`) → mcp-server (`create_conversation` 映射)。
- 评分模型所需的 `creator_performance` 字段已由底层链路补齐；本包按固定展示契约消费这些字段。
- **多店铺切换** 的底层授权与店铺选择模型已由 `mcp-remote` / `mcp-server` 提供；本包负责将自然语言意图映射到对应的 `authorize` 调用。

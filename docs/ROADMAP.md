# creator-outreach — 开发计划

> 已完成项按交付主题汇总；同一主题的连续迭代优先回写原条目，历史细项通过 Git 历史追溯。

## 已完成

- [x] **WorkBuddy 开放平台专家上架交付包**: 收敛 `plugin.json` 展示字段（`tags` 6→3、`quickPrompts` 1→3、`displayDescription.zh` 78→48 字、头像 1596→512），新增 `listing/`（根 `.mcp.json` 依赖声明 + 随包产品摘要 README）与 `scripts/`（从权威正文生成剥离 bootstrap 门禁的上架版 Agent 正文、字段校验与纯净打包），产出 78KB zip；测试补齐字数与标签口径断言，以及「权威正文保留分享链接门禁 / 上架版剥离该门禁」双向断言。同时为 `pack-expert.sh` 增加上架基线守卫，强制「包内无未提交改动 + HEAD 已打 tag」后才允许出包（`--skip-baseline-check` 仅作本地排查逃生开关），确保每次提审的 zip 均可追溯到不可变基线（2026-09-11）。
- [x] **相似达人推荐**: `find_similar_creators` MCP 工具（mcp-remote，含三路召回、候选预算和外部双达人相似度服务唯一评分）+ `tiktok-similar-creators` Skill（creator-outreach，含置信度与展示契约）；没有有效外部评分时返回错误，不提供本地评分替代（2026-08-02 ~ 2026-09-03）。
- [x] **已废弃宿主残留清理**: 删除安装测试中的废弃宿主兼容断言，并把 ROADMAP / CHANGELOG 中仍显式提及旧宿主的历史文案收敛为仅描述 WorkBuddy 路径，避免对当前读者暴露已废弃宿主（2026-07-21）。
- [x] **WorkBuddy 双通道交付与自测闭环**: 把交付拆成两条互不干扰的通道 —— 开放平台上架版仍是终端用户版本（由 WorkBuddy 下载物化，安装器只读其版本用于诊断），本地安装改为开发者自测路径（新增 `bootstrap --dev`，以 `tiktok-creator-outreach-dev@my-experts` + 独立显示名注册，并把全部 skill id 加 `-dev` 后缀，从根本上避免与平台版撞名遮蔽 —— WorkBuddy 对同名技能是「先加载者占用、后者静默跳过」且不报错）；`--dev` 从「只丢一个目录」升级为端到端激活（upsert `my-experts` 清单条目 + 物化版本化缓存 + 写 `installed_plugins.json` + 写 `enabledPlugins`，任一步失败整体回滚），每次重写缓存而不按版本跳过，确保自测永不跑旧代码，并把自测副本的代码来源（仓库根）记入登记表的 `sourceDir` —— 由此连来自 npm 发布包的静默更新也只从该仓库取材，不会用发布版覆盖未发布的改动（`bootstrap --check` 以 `local_channel_source_dir` 回报）；安装与静默更新都会清理旧版安装器留下的同名 `my-experts` 副本（它从未登记进市场清单、在列表里不可见，却照样参与扩展加载：真实机器上它与平台版在同一个进程里加载了同名技能，是「更新了包却仍在跑旧代码」的成因之一），判定按目录内 `plugin.json` 身份做、不误删他人的自定义专家；订正 `enabledPlugins` 的语义 —— 实测该值为 false 并不阻止加载，它只决定插件管理页显示的启用态，真正的隔离靠插件名与 skill id；五状态 bootstrap 门禁沿用，但 `activation_required` 改为由安装器自行推进加载位（不再引导用户去插件管理页点「更新」），生效判定读取 `installed_plugins.json` 的实际加载版本并校验缓存目录存在，不再把「已写入源码位」当成「已生效」；Windows 托管运行时通过 `node.exe` 直接加载 npm CLI，并自动迁移旧 `.cmd` / `.bat` 和过期运行时路径；重启后通过匹配 MCP 托管元数据和版本自动清除重启标记，避免状态死锁（2026-07-21 ~ 2026-09-14）。
- [x] **文档权威来源与客户端支持收敛**: 将 Agent 收敛为共享行为与跨 Skill 编排来源，Skills 分别维护搜索、分析和建联专项契约，References 维护评分公式、字段映射和话术模板；保留并标注非权威 Python 参考实现，包内文档收敛为产品/架构说明，客户端仅支持 WorkBuddy（2026-07-21 ~ 2026-07-22）。
- [x] **Agent、Skills、Rank 分数契约与建联模板基础**: 完成 TikTok Creator Outreach Agent、三项核心 Skill、Rank 分数消费口径与可解释推荐理由、A/B/C 建联话术，以及整批单选、默认值可编辑、通知栏前 15 字预览的利益前置 Offer 预设（2026-07-09 ~ 2026-07-22）。
- [x] **生产入口与商家错误引导**: 切换生产 MCP 域名，并按结构化错误类型分别引导店铺重新绑定、限流等待或修正输入，不暴露错误码（2026-07-15）。
- [x] **安装可靠性、MCP 可用性与凭证托管**: 明确支持客户端、安装前校验和 MCP 拉取方式，移除面向终端用户的 TikTok 凭证直连降级（2026-07-16）。
- [x] **本地运行环境自检与恢复**: 对明确的 Node.js / npm / npx 缺失或 Node.js 版本过低进行诊断，经用户确认后按平台引导官方 LTS 安装、复检并重启客户端（2026-07-20）。
- [x] **品牌定位、首次使用体验与对外口径统一**: 完成首轮能力引导、品牌入口优化、展示字段职责收敛、宣传手册精简，并统一 Tiky 的达人画像与小范围建联验证定位（2026-07-16 ~ 2026-07-20）。
- [x] **账号/店铺切换与授权确认体验**: 识别切换店铺 / 账号意图，店铺切换直接调用免登录确认页，账号切换才重新登录；授权成功后统一确认账号、店铺、国家和绑定品牌（2026-07-17）。
- [x] **搜索与 Rank 分数展示契约收敛**: 在精细画像筛选、渐进式引导和稳定分页基础上，将搜索规则收紧为当前店铺 SEA v2 类目树下的顶层或明确指定的直接子类目，使用店铺当地语言名称并校验达人地区；同时移除跨市场静态类目 ID，并统一搜索结果、Rank 分数、达人标识、WorkBuddy 交互与 HTML 契约；评分请求对 1 位及任意数量达人直接使用 `rank_creators` 返回的 `score`，Rank 不可用时不再本地回算旧评分模型（2026-07-17 ~ 2026-08-21）。

## 待开发

### 中优先级

- [ ] **配额感知**: 基于已知日配额上限（10,000/天）做预算提示；当远程工具返回 `quota_exhausted` 时停止并提示等待，不再承诺“每次操作前实时检查剩余额度”。
- [ ] **竞品达人搜索**: 输入竞品品牌名，复用 `search_creators` 与 Rank 分数契约筛选高潜力名单；hashtag 搜索仅作为实验性补充，不作为默认能力承诺。

### 低优先级

（当前无）

## 暂不做

- **消息监听（达人回复接收/分类/状态机）**: TikTok 无 GET 轮询，接收依赖 Webhook (Type 33)，且需消息落库；本包无数据库、禁止数据修改，暂不纳入本包范围。
- **A/B 测试建联话术**: 追踪回复率需要持久化发送记录和回复结果，超出当前包“无自有数据库、禁止数据修改”的约束。
- **导出功能**: 当前包聚焦对话式筛选、分析和建联流程，不再单独承诺 CSV/Excel 导出能力。

## 跨包依赖

- **`create_conversation` MCP 工具**（批量建联前置）已沿依赖链补齐：sdk (`createConversation`) → mcp-remote (`/mcp/tools/create-conversation`) → mcp-server (`create_conversation` 映射)。
- **Rank 分数字段** 已由底层链路补齐；本包按固定展示契约消费 1 位及任意数量候选的 `rank_creators` `rank`、`score` 与并列 Performance 事实。
- **多店铺切换** 的底层授权与店铺选择模型已由 `mcp-remote` / `mcp-server` 提供；本包负责将自然语言意图映射到对应的 `authorize` 调用。
